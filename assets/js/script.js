// Added the inputs for city form, histroy, current weather, and the 5-day forcast
const inputCity = $('#city-input');
const formCity = $('#city-form');
const history = $('#city-history');
const currentWeather = $('#current-weather');
const weatherForcast = $('#weather-forcast');

